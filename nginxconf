events {}

http {
    upstream backend {
       server localhost:3030;
       server localhost:3031;
    }


    #  This server accepts all traffic to port 8080 and passes it to the upstream.

    server {
       listen 8080;

       location / {
          proxy_pass http://backend;
       }
    }
}